export interface LibraryProps {
    prop: string
}

const DataPromise = async () => {

}

export default function Library({ prop }: LibraryProps) {
    return (
        <section>

        </section>
    )
}